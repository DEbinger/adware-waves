var adList = [

];

function addAd (path, name, numerator, denominator, kill, crash, loaded){
  var newAd = {

    path : path,
    name : name,
    n: numerator,
    d: denominator,
    kill : kill,
    crash : crash,
    loaded : loaded};

  adList.push(newAd);

}



